# docker images for running match day

## mysql
### start mysql container
<code>sudo docker-compose up</code>

### connect to running container
<code>sudo docker exec -it docker-images_db_1 bash</code>